Program to simplify 3D meshes using the edge collapse operation.

To compile and run:

$ make
$ ./reduce camel.off

Change raptor.off with any off file placed in the objects folder to use different models.
